<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aonproject.admin.category.dao.CategoryDAO">
	<!-- 카테고리 리스트 조회 -->
	<select id="categoryList" resultType="category" parameterType="category">
		/* category - categoryList */
		select category.ca_no, ca_name, ca_date 
		from category
	</select>
	
	
	<!-- 공통코드 리스트 조회 -->
	<select id="commonCodeList" resultType="commonCode" parameterType="commonCode">
		/* commonCode - commonCodeList */
		select cc_no, cc_name, cc_group, cc_desc, cc_date 
		from common_code
	</select>
</mapper>
